/// Contact configuration for VidyaRas enrollment
class ContactConfig {
  // Phone number for direct calls (with country code and + sign)
  static const String phoneNumber = '+917810906454';

  // Support email for reference (used in share message subject)
  static const String supportEmail = 'cs.aditya58@gmail.com';

  // Private constructor to prevent instantiation
  ContactConfig._();
}
